{
  "private": true,
  "scripts": {
    "init": "node init.mjs",
    "build": "set -eu; for w in 400; do svgo --config svgo.config.js --quiet --recursive $w; done"
  },
  "devDependencies": {
    "svgo": "2.8.0",
    "svgo-add-viewbox": "1.1.0"
  }
}
